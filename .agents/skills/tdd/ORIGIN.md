# Origin (clc-kewe)

This skill is the Matt Pocock `tdd` primitive re-admitted without rewrite.

- Catalog: https://github.com/mattpocock/skills
- Pin: commit `5b15a47f2d7150f545fbcacbfe381787fc0230dc`
- Upstream path: `skills/engineering/tdd/`
- Retirement record: `~/.agents/skills-retired/2026-08-22-mattpocock/tdd/`
  (retired with the unused Pocock set on 2026-08-22; Daily kept a reviewed
  subset that did not include `tdd` as a Workspace root — that root is clc-apsq)

`SKILL.md`, `tests.md`, `mocking.md`, and `agents/openai.yaml` are byte-identical
to that retired copy / pinned commit. Do not rewrite them.
