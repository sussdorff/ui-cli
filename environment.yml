name: ui-cli
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
    - typer>=0.12.0
    - httpx>=0.27.0
    - rich>=13.0.0
    - pydantic>=2.0.0
    - pydantic-settings>=2.0.0
    - python-dotenv>=1.0.0
